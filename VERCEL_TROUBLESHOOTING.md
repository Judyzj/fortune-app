# 🔧 Vercel 部署故障排除完整指南

## 🚨 当前问题
两个链接都打不开：
- https://fortune-app-git-main-judyans-projects.vercel.app/
- https://fortune-l78pi8uai-judyans-projects.vercel.app/

## 📋 完整检查清单

### 步骤 1: 检查 Vercel Dashboard 中的部署状态

1. **登录 Vercel**: https://vercel.com/dashboard
2. **找到项目**: `fortune-app`
3. **查看 Deployments 标签**

**检查每个部署的状态：**
- ✅ **Ready** (绿色) = 部署成功，应该可以访问
- ⏳ **Building** (黄色) = 正在构建，等待完成
- ❌ **Error** (红色) = 部署失败，需要查看日志
- 🔄 **Queued** (灰色) = 等待构建

### 步骤 2: 如果部署状态是 "Error"

#### A. 查看构建日志
1. 点击失败的部署
2. 展开 **"Build Logs"**
3. 查找错误信息

#### B. 常见错误及解决方案

**错误 1: `cd: frontend: No such file or directory`**
```
解决方案：
1. Settings → General → Build & Development Settings
2. 清空 Build Command 和 Install Command
3. 确认 Root Directory 为 `frontend`
4. Output Directory 设置为 `dist`
5. 保存并重新部署
```

**错误 2: `Cannot find module` 或依赖安装失败**
```
解决方案：
1. 检查 frontend/package.json 是否存在
2. 确认所有依赖都在 package.json 中
3. 尝试清除 Vercel 缓存后重新部署
```

**错误 3: 构建成功但网站空白**
```
解决方案：
1. 检查 Output Directory 是否为 `dist`（不是 `frontend/dist`）
2. 确认 index.html 在 dist 目录中
3. 检查路由配置（vercel.json 的 rewrites）
```

### 步骤 3: 正确配置项目设置

在 Vercel 项目设置中，**必须**按以下配置：

#### Settings → General

**Root Directory:**
- ✅ 设置为：`frontend`

**Build & Development Settings:**
- **Build Command**: 留空（让 Vercel 自动检测为 `npm run build`）
- **Install Command**: 留空（让 Vercel 自动检测为 `npm install`）
- **Output Directory**: `dist`（**不要**写 `frontend/dist`）
- **Development Command**: 留空或 `npm run dev`

#### Settings → Environment Variables

添加以下环境变量：
- **Key**: `VITE_API_BASE_URL`
- **Value**: `http://localhost:8000`
- **Environment**: 全选（Production, Preview, Development）

### 步骤 4: 重新部署

1. **进入 Deployments 页面**
2. **找到最新的部署**（无论成功或失败）
3. **点击右侧 "..." 菜单**
4. **选择 "Redeploy"**
5. **等待部署完成**（通常 1-2 分钟）

### 步骤 5: 验证部署

部署成功后：

1. **检查部署状态**：应该显示 "Ready"（绿色）
2. **点击部署链接**：应该能打开网站
3. **检查控制台**：打开浏览器开发者工具（F12），查看是否有错误

## 🔍 调试步骤

### 如果部署显示 "Ready" 但网站打不开：

1. **检查浏览器控制台**
   - 按 F12 打开开发者工具
   - 查看 Console 标签中的错误信息

2. **检查网络请求**
   - 在 Network 标签中查看失败的请求
   - 检查是否有 CORS 错误

3. **尝试清除浏览器缓存**
   - Ctrl+Shift+R (Windows) 或 Cmd+Shift+R (Mac)
   - 或使用无痕模式访问

4. **检查 Vercel 域名**
   - 确认使用的是正确的部署链接
   - 每个部署都有唯一的链接

## 🆘 如果还是不行

### 方案 A: 删除项目重新导入

1. **删除当前项目**（Settings → Delete Project）
2. **重新导入**：
   - Add New Project
   - 选择 `fortune-app` 仓库
   - **在导入时设置**：
     - Root Directory: `frontend`
     - Build Command: 留空
     - Install Command: 留空
     - Output Directory: `dist`
   - 添加环境变量
   - 点击 Deploy

### 方案 B: 检查 GitHub 仓库

确认 GitHub 仓库中有所有文件：
- 访问：https://github.com/Judyzj/fortune-app
- 确认 `frontend/` 目录存在
- 确认 `frontend/package.json` 存在
- 确认 `frontend/index.html` 存在

## 📞 需要帮助时提供的信息

如果问题仍然存在，请提供：

1. **Vercel Dashboard 截图**：
   - Deployments 页面
   - 失败的部署详情
   - 构建日志

2. **项目设置截图**：
   - Root Directory 设置
   - Build & Development Settings
   - Environment Variables

3. **浏览器控制台错误**（如果有）

## ✅ 成功标志

部署成功后，你应该看到：
- ✅ 部署状态：Ready（绿色）
- ✅ 可以访问网站链接
- ✅ 网站正常显示（即使 API 可能还连不上，前端界面应该能显示）
